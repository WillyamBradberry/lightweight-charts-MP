Successfully created the comprehensive theme surface map for MP-charts-toolkit at `docs/theme/theme-surface-map.md`. 

The document includes:
- **TradingView CSS Custom Properties (SSOT)** — all 14 `--tv-color-*` variables with roles and defaults.
- **Hardcoded Palette** — 12 specific HEX colors used across components (brand, chart, line-tools, alerts).
- **Component Surface Map** — detailed breakdown of Layout, Left/Right Toolbars, Chart Component/Grid, Topbar, Toast, Alerts Panel, and Line Tools Floating Toolbar with exact CSS properties, values, and class names.
- **Spacing System** — fixed widths (52px toolbars, 300px alerts), heights (38px topbar, 48px panels), padding/margin contexts, border-radius scale, and box-shadow definitions.
- **Typography** — font stack and per-component font-size/weight mappings.
- **Z-Index Scale**, **Animations/Transitions**, **Borders/Grid gaps**, **Interactive States** (hover/active/disabled).
- **Summary Palettes** for quick reference of all HEX values and CSS variables.

The file is ready for use as a single source of truth for theming, styling, or UI implementation within the MP-charts-toolkit project.