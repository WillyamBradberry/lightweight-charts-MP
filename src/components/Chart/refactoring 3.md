Следующий шаг.

Создай:
1. `builders/chartOptionsBuilder.js` — чистая функция, которая собирает options для чарта
2. `hooks/useChartOptions.js` — хук, который применяет эти options и обновляет их при изменении пропсов

После этого обнови `ChartComponent.jsx` и `useChart.js` (если он уже есть).

Покажи:
- Код `chartOptionsBuilder.js`
- Код `useChartOptions.js`
- Diff по `ChartComponent.jsx`