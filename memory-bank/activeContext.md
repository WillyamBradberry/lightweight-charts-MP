Ready for next task.

Work Dir: libs/MP-charts-toolkit
Focus: Spec-First restructure + Project Matrix foundation

Last task: Reworked floating toolbar to EXACT legacy structure — real SVG icons,
fixed vertical-stack bug (removed display:block, use .hidden class), one drag handle.
Validated via npm run build.
